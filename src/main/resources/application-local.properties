# Server
server.port=8001

# Database
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://host.docker.internal:3306/dbservice?serverTimezone=Asia/Jakarta&useLegacyDatetimeCode=false
spring.datasource.username=uservice
spring.datasource.password=P@ssw0rd1!

# Persistance
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update

# Domain
static.domain=http://s.standalone-base-framework.id

# File Upload
upload.file.root.path=/Users/rio.bastian/Documents/app/workspace

# Logging
logging.config=classpath:log/logback-local.xml

# Memcache
cache.address.memcache=host.docker.internal:11211

# Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
